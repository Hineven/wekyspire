1. 你主要负责编写前端渲染代码，但记得后端的游戏逻辑也有其重要作用。在有必要时，你需要参考后端代码来理解整个游戏的逻辑，以更好完成前端代码的编写。
2. 不要混淆前后端的`gameState`，UI使用`displayGameState`，后端使用`backendGameState`。
3. 前后端状态交互通过`animationSequencer.js`完成，它将后端对`backendGameState`的状态修改和动画指令输入动画队列，由一个单独协程消耗此队列元素通知前端。
4. 编码时，请同时考虑优化代码结构设计，如果有可以抽离共用的逻辑，尽量抽离出来，避免重复代码。